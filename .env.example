# Used for canonical URLs, Open Graph metadata, and sitemap.xml in production.
# Replace this with the production domain before deployment.
SITE_URL=https://northshop.agency

# Server-only Resend configuration for the store review form.
# Verify northshop.agency in Resend before using its addresses as a sender.
RESEND_API_KEY=re_replace_me
RESEND_FROM_EMAIL=North/Shop Website <leads@northshop.agency>
RESEND_TO_EMAIL=hello@northshop.agency

